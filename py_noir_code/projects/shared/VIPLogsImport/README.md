VIPLogsImport
===

Project used for manually downloading VIP logs of specific execution

# Parameters

- workflow identifier : workflow-1rx1c8 : VIP execution identifier, accessible from web interface when execution didn't crash, else it has to be retrieved directly into the database (not accessible in the VIP web page)